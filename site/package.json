{
  "name": "personal-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "sync-raw": "rm -rf raw && cp -r ../raw ."
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
